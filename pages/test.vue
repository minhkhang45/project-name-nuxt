<script setup>
const slides = ref(Array.from({ length: 100 }, () => {
  const r = Math.floor(Math.random() * 256)
  const g = Math.floor(Math.random() * 256)
  const b = Math.floor(Math.random() * 256)
  // Figure out contrast color for font
  const contrast = r * 0.299 + g * 0.587 + b * 0.114 > 186 ? 'black' : 'white'

  return { bg: `rgb(${r}, ${g}, ${b})`, color: contrast }
}))
</script>

<template>
  <div>
    <Swiper class="max-w-[500px]" :key="1" :modules="[SwiperAutoplay, SwiperNavigation]" :slides-per-view="3" :space-between="20" navigation
      :loop="true" :autoplay="{
        delay: 4000,
        disableOnInteraction: true,
      }">
      <SwiperSlide v-for="(slide, idx) in slides" :key="idx"
        :style="`background-color: ${slide.bg}; color: ${slide.color}`">
        {{ idx }}
      </SwiperSlide>
    </Swiper>
    ti loh
  </div>
</template>

<style>
.swiper-slide {
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 18px;
  height: 20vh;
  font-size: 4rem;
  font-weight: bold;
  font-family: 'Roboto', sans-serif;
}

.swiper-wrapper {
  min-width: 100vh;
  width: 100vh;
}

.swiper-cards {
  width: 240px;
  height: 240px;
}

.swiper-cards .swiper-slide {
  border-radius: 6px;
  border: 1px solid black;
}
.swiper-button-next,
.swiper-button-prev {
    min-width: 50px;
    min-height: 50px;
    border-radius: 50%;
    background: #00bdfe;
    opacity: 0.75;
    color: #fff;
    transition: all .3s;
}
.swiper-button-next:hover,
.swiper-button-prev:hover {
  opacity: 1;
  background: #00bdfe;
}
.swiper-button-prev::after,
.swiper-button-next::after {
    font-size: 20px;
    font-weight: 900;
}
</style>
